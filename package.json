{
  "name": "personal-budget",
  "version": "1.0.0",
  "scripts": {
    "install": "cd packages && cd client && npm install && cd ../server && npm install",
    "start": "lerna run start --stream",
    "build": "cd packages && cd client && npm run build && cd ../server && npm run build"
  },
  "author": "Linamikaela21",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
